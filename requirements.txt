# NIDS NetFlow Pipeline - Dependencies
# Ottimizzato per chunk-based processing su grandi dataset

# Core Data Processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=12.0.0          # Per Parquet (veloce e compresso)

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0

# System Monitoring
psutil>=5.9.0            # Per monitoraggio RAM

# Optional (per sviluppo)
jupyter>=1.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
